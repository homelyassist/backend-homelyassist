package com.homelyassist.repository.db;

import com.homelyassist.model.db.ConstructorWorker;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ConstructorWorkerRepository extends JpaRepository<ConstructorWorker, Long> {
}
